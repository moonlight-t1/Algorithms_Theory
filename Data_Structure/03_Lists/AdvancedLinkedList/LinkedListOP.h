#ifndef _LINKEDLIST_OP_H_
#define _LINKEDLIST_OP_H_

#include "LinkedList.h"

void iteratedLinkedList(LinkedList *pList);                    // 순회 함수
void concatLinekdList(LinkedList *pListA, LinkedList *pListB); // 연결시키는 함수
void reverseLinkedList(LinkedList *pList);                     // 역순으로 만드는 함수

#endif